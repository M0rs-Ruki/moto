# Backend Environment Variables

# ============================================
# Server Configuration
# ============================================
# Port on which the Express server will run
PORT=8000

# CORS allowed origin (frontend URL)
# In production, set this to your frontend domain
CORS_ORIGIN=http://localhost:3000

# ============================================
# Database Configuration
# ============================================
# PostgreSQL database connection string (Prisma)
# Format: postgresql://user:password@host:port/database?schema=public
DATABASE_URL=postgresql://user:password@localhost:5432/moter?schema=public

# ============================================
# Authentication
# ============================================
# JWT secret key for token generation and verification
# Generate a strong random string (minimum 32 characters)
# Must match the JWT_SECRET in frontend .env
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# ============================================
# WhatsApp API Configuration
# ============================================
# WhatsApp API base URL (default: chati.ai)
WHATSAPP_API_URL=https://api.chati.ai/v1/public/api

# WhatsApp API bearer token (from chati.ai dashboard)
WHATSAPP_API_TOKEN=your-whatsapp-api-bearer-token

# WhatsApp API access key (from chati.ai dashboard)
WHATSAPP_API_ACCESS_KEY=your-whatsapp-api-access-key

# ============================================
# Cron Jobs
# ============================================
# Secret key for authenticating cron job requests
# Used to secure the /api/cron endpoint
CRON_SECRET=your-cron-secret-key-change-this-in-production

# ============================================
# Vercel Blob Storage (Optional)
# ============================================
# Set to "1" if deploying on Vercel
VERCEL=0

# Vercel Blob storage token for profile picture uploads
# Only needed if using Vercel Blob storage
BLOB_READ_WRITE_TOKEN=your-vercel-blob-token

# ============================================
# Environment
# ============================================
# Node environment: development, production, or test
NODE_ENV=development
